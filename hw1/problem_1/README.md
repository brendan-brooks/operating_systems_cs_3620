# Fibonacci Calculator

Computes *n* Fibonacci numbers (where *n* is an int between 1 and 92) in O(n) time.

### Drawbacks:
Limited to n ≤ 92 by the size of the `long long int` data type. Shows the big ideas without implementing a 
more complicated, dynamic-size data structure (like Python 3's `int` type).